{
  "mcpServers": {
    "devcontext": {
      "command": "uv",
      "args": [
        "--directory",
        "/absolute/path/to/devcontext-mcp",
        "run",
        "devcontext-mcp"
      ],
      "env": {
        "DEVCONTEXT_BACKEND_MODE": "mock"
      }
    }
  }
}
